{
  "name": "fastify-auth-middleware",
  "license": "MIT",
  "description": "Fastify plugin to allow passing RBAC based JWT tokens as a route middleware.",
  "version": "1.0.8",
  "main": "dist/index.js",
  "types": "dist",
  "repository": {
    "type": "git",
    "url": "https://github.com/fawzy-tat/fastify-auth-middleware"
  },
  "scripts": {
    "build": "tsc -p ."
  },
  "devDependencies": {
    "@types/node": "^20.3.1",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "fastify": "^4.18.0",
    "fastify-plugin": "^4.5.0",
    "jwt-decode": "^3.1.2"
  }
}
